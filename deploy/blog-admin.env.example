# Copy to /etc/gexian-blog-admin.env on the server, set safe permissions, and
# replace every value before starting the systemd service.
ADDR=127.0.0.1:8080
ADMIN_USER=
ADMIN_PASS=
SESSION_SECRET=
ADMIN_BASE_PATH=/write
PUBLIC_BASE_URL=/
